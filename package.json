{
  "name": "link-lib",
  "version": "1.0.0",
  "description": "A tiny Node script that **links a local library into your app** the right way—idempotently, Windows-safe, and without clobbering your version spec when you unlink.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "semver": "^7.7.2"
  }
}
